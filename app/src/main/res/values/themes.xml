<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Shopping" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/black</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <item name="font">@font/roboto_regular</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>
    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />
    <style name="Active.TextAppearnce" parent="@android:style/TextAppearance">
        <item name="android:textSize">@dimen/_11sdp</item>
        <item name="android:textColor">@color/black</item>
        <item name="android:paddingTop">50dp</item>
        <item name="fontFamily">@font/roboto_regular</item>
    </style>

    <style name="CollapsingToolbarLayoutCollapseTextStyle" parent="ThemeOverlay.AppCompat.Light">
        <item name="android:textColor">#000330</item>
        <item name="android:textSize">@dimen/_16sdp</item>
    </style>

    <style name="CollapsingToolbarLayoutExpandedTextStyle" parent="ThemeOverlay.AppCompat.Light">
        <item name="android:textColor">#000330</item>
        <item name="android:textSize">@dimen/_1sdp</item>
    </style>

    <style name="Inactive.TextAppearnce" parent="@android:style/TextAppearance">
        <item name="android:textSize">@dimen/_11sdp</item>
        <item name="android:textColor">#C6C4C4</item>
        <item name="fontFamily">@font/roboto_regular</item>
        <item name="android:paddingTop">50dp</item>
    </style>

</resources>